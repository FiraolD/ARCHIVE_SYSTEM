# frontend/.env
# Use your local IP instead of localhost
VITE_API_URL=http://10.1.12.41:7000/api
# Or use relative path (if frontend and backend are on same machine)
# VITE_API_URL=/api  # Would need proxy configuration